.container
  .banner
    チャットグループが作成されました。
  .left-contents
    .user-menu
      %h2 Shuhei Kaneko
      =link_to "" do
        =fa_icon("cog 2x")
      =link_to "" do
        =fa_icon("edit 2x")
    .group-menu
      -# 以下はテーブル実装時にeachで書き直す予定
      - for i in 1..3
        .group-menu__content
          %h2 sample_group#{i}
          %p sample_message#{i}
        -# ここまで

  .right-contents
    .selected-group
      .selected-group__name
        %h2 sampleグループ
        %p Menbers: sample太郎, sample子, sample平
      =link_to "" do
        %button.edit-button Edit
    .right-contents__chat-space
      .chat-space__tweet
        %p.tweet--name sample太郎
        %p.tweet--time 2017/02/24 15:42
        %p.tweet say sample
      .chat-space__writeme
        =form_tag do
          .textbox-frame
            = text_field_tag :text,nil,placeholder: '   type a message'
            = link_to "" do
              = fa_icon("image 2x")
          = submit_tag('send',id: 'send')




